version: 2

jobs:
  build:
    docker:
      - image: circleci/python:3.6

    steps:
      - checkout
      - run: pip install --user pandas scikit-learn nilearn joblib POT Cython
      - run: pip install --user .
      - run: set-o pipefail && pytest 2>&1 | tee log.txt
